{
  "name": "cinesopa-webapp",
  "description": "client and server",
  "authors": "EzKorry",
  "version": "0.1.0",
  "main": "pathToMain",
  "dependencies": {},
  "type": "module",
  "scripts": {
    "start-server": "cd server && npm run start",
    "dev-client": "cd client && npm run serve",
    "install": "npm install -g copyfiles pm2 && cd server && npm install && cd ../client && npm install",
    "build-client": "cd client && npm run build",
    "copy": "copyfiles -u 2 \"client/dist/**/*\" \"server/dist\"",
    "start": "npm run install && npm run build-client && npm run copy && npm run start-server"
  }
}
